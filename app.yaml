env: standard
runtime: nodejs12
service: default
handlers:
  - url: /
    static_files: dist/index.html
    upload: dist/index.html
    secure: always

  - url: /
    static_dir: dist
    secure: always
